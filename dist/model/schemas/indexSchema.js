"use strict";
// import { Sequelize } from 'sequelize';
// import  { initializeEventModel }  from './eventSchema';
// import dbConfig from '../../database/dbConfig';
// import { initializeBookingModel } from './bookingSchema';
// import { initializeWaitingListModel } from './waitingListSchema';
// import { initializeUserModel } from './userSchema';
// const sequelize = new Sequelize(dbConfig);
// Initialize models
// initializeEventModel(sequelize);
// initializeUserModel(sequelize); 
// initializeBookingModel(sequelize);
// initializeWaitingListModel(sequelize);
// Sync the database (if needed)
// sequelize.sync({ alter: true }).then(() => {
//     console.log('Database synced successfully');
//   }).catch((error) => {
//     console.error('Error syncing database:', error);
//   });
// export { sequelize };
